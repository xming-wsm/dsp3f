#include "dsp3f.h"
#include <stdint.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
  // double a[10] = {1, 3, 0, 9, 8, 7, 77, 88, 99, 1001};
  // double b[8] = {1, 2, 3, 4, 5, 6, 15, 42};
  //
  // int convLen = 0;
  // double *result = dsp3f_basic_conv(a, 10, b, 8, &convLen);
  // // double *result = dsp3f_basic_conv(a, 5, b, 3);
  // for (int i = 0; i < convLen; i++) {
  //   printf("%lf\n", result[i]);
  // }
  // free(result);
  //

  return EXIT_SUCCESS;
}
